-- Autogenerated from sql_create.template 
-- Time: 2021-03-15 20:21:44.627972
-- Database: mysql

CREATE TABLE Student (
    id varchar PRIMARY KEY,
    firstName varchar,
    lastName varchar,
    group_id_student varchar FOREIGN KEY REFERENCES Group(id),
    mark_id_student varchar FOREIGN KEY REFERENCES Mark(id),
);

CREATE TABLE Group (
    id varchar PRIMARY KEY,
    name varchar UNIQUE NOT NULL,
);

CREATE TABLE Subject (
    id varchar PRIMARY KEY,
    title varchar UNIQUE NOT NULL,
    mark_id_subject varchar FOREIGN KEY REFERENCES Mark(id),
);

CREATE TABLE Subject_Group (
    subject_id varchar FOREIGN KEY REFERENCES Subject(id),
    group_id varchar FOREIGN KEY REFERENCES Group(id),
);

CREATE TABLE Mark (
    id varchar PRIMARY KEY,
    date datetime,
    mark number,
);

