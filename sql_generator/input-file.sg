database=mysql

fields MyFields {
    hello varchar [pk]
    naziv varchar
}

entity Fakultet extends MyFields {
    naziv varchar [pk]
    godina_osnivanja number [unique notnull]
    grad varchar [notnull]
}

entity Fields2 extends MyFields {
    naziv varchar [pk]
}


entity Predmet {
    naziv varchar [pk]
    broj_bodova number [notnull]
}

entity Student {
    id integer [pk]
    indeks integer
    *..* Fakultet
    *..* Predmet
}
