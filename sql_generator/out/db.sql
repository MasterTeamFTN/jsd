-- Autogenerated from sql_create.template 
-- Time: 2021-03-30 15:45:28.193312
-- Database: mysql

CREATE TABLE MyFields (
    hello varchar PRIMARY KEY,
    naziv varchar,
);

CREATE TABLE Fakultet (
    naziv varchar PRIMARY KEY,
    godina_osnivanja number UNIQUE NOT NULL,
    grad varchar NOT NULL,
    myfields_hello varchar FOREIGN KEY REFERENCES MyFields(hello),
);

CREATE TABLE Fields2 (
    naziv varchar PRIMARY KEY,
    myfields_hello varchar FOREIGN KEY REFERENCES MyFields(hello),
);

CREATE TABLE Predmet (
    naziv varchar PRIMARY KEY,
    broj_bodova number NOT NULL,
);

CREATE TABLE Student (
    id integer PRIMARY KEY,
    indeks integer,
);

CREATE TABLE Student_Fakultet (
    student_id integer FOREIGN KEY REFERENCES Student(id),
    fakultet_naziv varchar FOREIGN KEY REFERENCES Fakultet(naziv),
);

CREATE TABLE Student_Predmet (
    student_id integer FOREIGN KEY REFERENCES Student(id),
    predmet_naziv varchar FOREIGN KEY REFERENCES Predmet(naziv),
);

