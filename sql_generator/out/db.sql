-- Autogenerated from sql_create.template 
-- Time: 2021-03-30 13:47:33.114896
-- Database: mysql

CREATE TABLE Student (
    id number PRIMARY KEY,
    indeks varchar UNIQUE NOT NULL,
    ime varchar,
    prezime varchar,
    predmet_naziv_diplomski varchar FOREIGN KEY REFERENCES Predmet(naziv),
    fakultet_id_prvazeljazaupis varchar FOREIGN KEY REFERENCES Fakultet(id),
    fakultet_id_drugazeljazaupis varchar FOREIGN KEY REFERENCES Fakultet(id),
);

CREATE TABLE Predmet (
    naziv varchar PRIMARY KEY,
    opis varchar NOT NULL,
);

CREATE TABLE Fakultet (
    id varchar PRIMARY KEY,
    naziv varchar,
);

CREATE TABLE Student_Predmet (
    student_id number FOREIGN KEY REFERENCES Student(id),
    predmet_naziv varchar FOREIGN KEY REFERENCES Predmet(naziv),
);

